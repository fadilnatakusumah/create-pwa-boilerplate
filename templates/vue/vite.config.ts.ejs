// templates/vue/vite.config.ts.ejs

import { defineConfig } from 'vite'
import vue from '@vitejs/plugin-vue'
import { VitePWA } from 'vite-plugin-pwa'
// tailwindCSSViteConfig


// https://vitejs.dev/config/
export default defineConfig({
  plugins: [
    // tailwindCSSFunctionViteConfig
    vue(),
    // 🔑 PWA Plugin Configuration
    VitePWA({
      registerType: 'autoUpdate',
      strategies: 'injectManifest', // We use injectManifest for custom push handlers
      srcDir: 'src',
      filename: 'sw.js',
      
      manifest: {
        name: '<%= pwa.name %>',
        short_name: '<%= pwa.shortName %>',
        theme_color: '<%= pwa.themeColor %>',
        // ... (rest of PWA metadata)
      },
      workbox: {
        // ... (Workbox config)
        clientsClaim: true,
        skipWaiting: true,
      },
    })
  ],
  resolve: {
    alias: {
      '@': '/src',
    },
  },
})